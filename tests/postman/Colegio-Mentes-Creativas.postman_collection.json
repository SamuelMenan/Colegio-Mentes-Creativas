{
  "info": {
    "name": "Colegio-Mentes-Creativas - Integración",
    "_postman_id": "2f6d1c44-0b34-46d5-a6fe-col",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Root (200 HTML)",
      "request": { "method": "GET", "url": "{{baseUrl}}/" },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status 200', () => pm.response.to.have.status(200));",
              "pm.test('content-type HTML', () => pm.expect(pm.response.headers.get('content-type')).to.include('text/html'));",
              "pm.test('tiempo < 1500ms', () => pm.expect(pm.response.responseTime).below(1500));"
            ]
          }
        }
      ]
    },
    {
      "name": "Vista 3D (200 HTML)",
      "request": { "method": "GET", "url": "{{baseUrl}}/construccion-bloques3D" },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status 200', () => pm.response.to.have.status(200));",
              "pm.test('content-type HTML', () => pm.expect(pm.response.headers.get('content-type')).to.include('text/html'));"
            ]
          }
        }
      ]
    },
    {
      "name": "Vista bloques (200 HTML)",
      "request": { "method": "GET", "url": "{{baseUrl}}/construccion-bloques" },
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": [
              "pm.test('status 200', () => pm.response.to.have.status(200));",
              "pm.test('content-type HTML', () => pm.expect(pm.response.headers.get('content-type')).to.include('text/html'));"
            ]
          }
        }
      ]
    }
  ]
}